import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  themeIconContainer: {
    flexDirection: 'row',
    justifyContent: 'flex-end',
    marginBottom: 24,
  },
  themeIcon: {
    height: 30,
    width: 30,
  },
});
